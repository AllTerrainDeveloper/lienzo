{
	"name": "lienzo",
	"version": "0.1.0",
	"private": true,
	"description": "A non-destructive, GPU-accelerated image editor for the WordPress media library.",
	"license": "GPL-2.0-or-later",
	"type": "module",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "npm run build:bundle && npm run deploy",
		"build:bundle": "vite build --mode development && vite build --mode production",
		"dev": "vite build --mode development --watch",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"env:start": "wp-env start",
		"env:stop": "wp-env stop",
		"test:php:install": "wp-env run tests-cli --env-cwd=wp-content/plugins/lienzo composer install",
		"test:php": "wp-env run tests-cli --env-cwd=wp-content/plugins/lienzo vendor/bin/phpunit --configuration tests/phpunit/phpunit.xml.dist",
		"deploy": "node bin/deploy.mjs",
		"plugin:build": "npm run typecheck && npm run test && npm run build:bundle",
		"plugin:check": "node bin/plugin-check.mjs",
		"plugin:package": "npm run plugin:build && node bin/package.mjs",
		"plugin:release": "npm run plugin:build && npm run plugin:check && node bin/package.mjs"
	},
	"devDependencies": {
		"@wordpress/env": "^10.17.0",
		"jsdom": "^25.0.1",
		"pixi.js": "^8.6.6",
		"typescript": "^5.7.2",
		"vite": "^5.4.11",
		"vitest": "^2.1.8"
	}
}
